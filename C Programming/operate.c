#include <stdio.h>
int main()
{
    int one, two, sum;
    printf("Ennter number one and two: ");
    scanf("%d %d", &one, &two);
    sum = one + two;
    printf("Adiition is: %d\n", sum);
    printf("%d", 3 + 7);
    return 0;
}